## Unreleased

- Add `set_active_database` and `get_active_database` language model tools so the
  chat agent can discover and switch the active workspace cluster/database,
  enabling cross-cluster investigations.

## v1.x

1.0.1

- Initial release
